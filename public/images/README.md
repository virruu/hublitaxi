# Site images

Static assets served from `/images/...` on the live site.

| Folder / file | Purpose                              |
| ------------- | ------------------------------------ |
| `fleet/`      | Vehicle photos for the fleet section |
| `og.jpg`      | Social share preview (1200 × 630)    |

- Fleet naming: `fleet/README.md`
- Social / OG image: `og-README.md` (path also set in `src/data/site.ts` → `ogImage`)
